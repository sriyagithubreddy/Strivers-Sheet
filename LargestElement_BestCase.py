from typing import List

class Solution:
    def largest(self, arr: List[int]) -> int:
        # The function finds the largest element in the input list 'arr'
        
        # Use Python's built-in max() function to return the largest element in the array
        # The max() function automatically traverses the entire array and returns the largest value.
        return max(arr)

